# Application dependencies

require:
    - play
    - play -> scaffold 0.1
    - custom -> api 0.1

repositories:
    - playCustomModules:
        type:       local
        artifact:   ${application.path}/repository/[organisation]/[module]-[revision].zip
        contains:
          - custom -> *