# Application dependencies

require:
    - play
    - play -> scaffold 0.1
    - api -> api 0.1

repositories:
    - playCustomModules:
        type:       local
        artifact:   ${application.path}/repository/[module]
        contains:
          - api -> *